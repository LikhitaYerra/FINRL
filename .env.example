# Copy to .env (never commit .env — it is gitignored).

# Out-of-sample news scoring (`score_oos_news.py`) via OpenRouter.
OPENROUTER_API_KEY=
# Optional override (default is a free-tier model id in code).
# OPENROUTER_MODEL=openai/gpt-oss-20b:free
